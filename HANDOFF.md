# 인수인계 — juyoung-page

다른 세션·다른 계정에서 이 사이트를 이어받을 때 읽는 문서입니다.
구조와 규칙은 [CLAUDE.md](CLAUDE.md) 에 있고, 이 문서는 **왜 그렇게 되어 있는지와 남은 일**을 다룹니다.

최종 갱신: 2026-08-06 (React 전환 완료 시점)

---

## 0. 세션 시작할 때

**작업 폴더를 `juyoung-web` 이 아니라 `juyoung-web/juyoung-page` 로 열어야 합니다.**
상위 폴더는 여러 사이트를 담는 컨테이너이고, 다른 사이트는 다른 세션에서 작업 중일 수 있습니다.

```
C:\Users\HP\Desktop\juyoung-web\juyoung-page      ← 여기
```

```bash
npm install     # 처음 한 번
npm run dev     # http://localhost:5220/juyoung-page/
```

---

## 1. 무엇을 만든 사이트인가

김주영(Kim Juyoung)의 **개인 포트폴리오**. 채용 담당자가 보는 것을 전제로 합니다.
Unity 기반 전시 인터랙션 / 게임 / AR·VR / IoT 프로젝트 **30건**의 기록이 핵심입니다.

| 항목 | 값 |
|---|---|
| 공개 주소 | https://cooldae.github.io/juyoung-page/ |
| 저장소 | https://github.com/cooldae/juyoung-page (public) |
| 기술 | Vite + React 19 + TypeScript + React Router 7 |
| 배포 | **GitHub Actions** (`.github/workflows/deploy.yml`) |
| git 계정 | cooldae / yegod1101@gmail.com (전역 설정됨) |

`main` 에 push 하면 자동 빌드·배포됩니다. **보통 40초~2분.**
빌드나 타입 검사가 실패하면 배포가 멈추므로 깨진 사이트가 올라가지 않습니다.

---

## 2. 이력

| 시점 | 내용 |
|---|---|
| v1 | 순수 HTML/CSS/JS, 빌드 없음. 태그 `v1-warm-brown`, 브랜치 `backup/design-v1` 로 보존 |
| v2 | React + TypeScript 전환. 디자인·콘텐츠는 그대로 두고 구조만 이관 |

**되돌리려면** `git checkout backup/design-v1 -- .`
바탕화면에 `juyoung-page_backup_v1_2026-08-06.zip` (git 히스토리 포함) 도 있습니다.

전환 이유는 사용자가 디자인을 크게 바꾸고 싶어했고, 코드 편집·컴포넌트 분리가 더 편해서입니다.
"React 가 디자인을 바꿔주지는 않는다"는 점은 사용자도 인지한 상태에서 결정했습니다.

---

## 3. 핵심 설계 — 왜 이렇게 만들었나

프로젝트가 30건이고 계속 늘어납니다. **프로젝트마다 페이지를 만들면 안 됩니다.**

```
데이터 파일 1개  +  상세 템플릿 1개  =  프로젝트 무한개
```

- `src/data/projects.ts` 의 `PROJECTS` 배열이 **유일한 원본**
- `src/pages/ProjectDetail.tsx` 하나가 모든 상세 페이지를 그림
- 필터 분류도 데이터에서 자동 생성 (`src/lib/projects.ts`)

**이 구조를 깨지 마세요.** 사용자의 최우선 요구가
*"제가 직접 추가할 수 있는 구조로 만들어줬으면 좋겠습니다 업데이트가 쉽게"* 였습니다.
프로젝트 추가는 `src/data/projects.ts` 에 블록 하나 붙여넣기로 끝나야 합니다.

---

## 4. 디자인 방향 (사용자 원문)

> "따뜻한 연한 갈색 톤의 분위기에 인터렉션이 어느정도 들어간 페이지면 좋겠습니다.
> 보는 사람이 피곤하거나 불편하지 않은 느낌으로 원합니다."

- 아이보리 `#f7f2e8` / 브라운 `#3b3229` / 포인트 캐러멜 `#a97b53`
- **순백·순검정 안 씁니다.** 눈 피로 때문입니다
- 다크 모드도 차가운 회색이 아니라 **따뜻한 다크 브라운**
- **스크롤 가로채기 · 상시 애니메이션 금지**, `prefers-reduced-motion` 존중

---

## 5. 진행 중 내려진 결정과 이유

| 결정 | 이유 |
|---|---|
| hero 큰 문장 = `김주영의 포트폴리오` | 추상적인 문구를 8개 제안했으나 전부 반려. 사용자가 직접 정한 방향 |
| hero 소개 문단 없음 (`intro: ""`) | 위 결정에 따라 비움. 값을 넣으면 다시 나타남 |
| Skills 를 hero 에 표시 | 사용자 요청 ("서브는 사용했던 기술을 목록으로") |
| About 에서 Skills 카드 제거 | 같은 태그 20개가 두 번 나와서. **다시 만들지 마세요** |
| 필터를 회사 → 분류로 변경 | 사용자 요청. 일하는 방식이 실제로 갈리는 지점으로 묶음 |
| 분류를 여러 개 붙임 | 컬러투라이프는 게임이자 드로잉, DMZ 포토존은 전시이자 AR |
| 학력을 About 카드로, 학위를 위에 | 채용 기본 확인 항목이지만 주인공은 프로젝트 |
| 영상 화살표를 플레이어 아래 | 위에 겹치면 유튜브 자체 컨트롤을 가림 |
| `youtube-nocookie.com` | 재생 전까지 유튜브 추적 쿠키가 안 심김 |
| TypeScript 채택 | 사용자가 C# 개발자라 개념이 익숙하고, 30건 데이터의 오타를 저장 시점에 잡음 |
| 주소를 `/project/<slug>` 로 | 사용자 요청 ("주소 형식도 깔끔하면 좋을 것 같긴해요") |

---

## 6. 함정 — 반드시 알아야 할 것

### 6-1. `vite.config.ts` 의 `base` 를 건드리지 마세요

`/juyoung-page/` 하위 경로 배포입니다. 틀리면 CSS·JS·이미지가 전부 404 납니다.
이미지 경로는 `src/lib/media.ts` 의 `imageSrc()` 로만 만드세요 (`import.meta.env.BASE_URL` 사용).

### 6-2. `public/404.html` 을 지우지 마세요

GitHub Pages 는 `/project/xxx` 를 모릅니다. 흐름은 이렇습니다.

```
/juyoung-page/project/pado-art-museum
  → GitHub Pages 가 404 응답 + 404.html 반환
  → 404.html 이 /juyoung-page/?redirect=project%2Fpado-art-museum 로 이동
  → src/main.tsx 가 진짜 주소로 복원
```

**콘솔에 404 가 한 번 찍히는 것은 정상입니다.** 이 방식의 구조적 특성입니다.

⚠️ **여기서 한 번 버그가 났었습니다.** 404.html 은 쿼리의 `?` 를 `&` 로 바꿔서 넘깁니다
(`project.html&id=xxx`). 복원할 때 첫 `&` 를 다시 `?` 로 돌려놓지 않으면 경로에 `&` 가
그대로 남아 아무 프로젝트도 안 잡힙니다. `src/main.tsx` 의 `normalizeUrl()` 참고.

### 6-3. 브라우저·Pages 캐시 때문에 자주 헷갈립니다

배포는 됐는데 화면이 안 바뀌는 일이 **매우 자주** 있습니다.
**서버 내용을 먼저 확인하고 판단하세요.** 화면만 보고 "배포 실패"라고 결론 내리면 틀립니다.

```powershell
# 서버가 실제로 주는 파일 확인
(Invoke-WebRequest "https://cooldae.github.io/juyoung-page/" -UseBasicParsing).Content -match 'index-\w+\.js'
```

브라우저에서는 주소에 `?cb=아무숫자` 를 붙이면 캐시를 우회할 수 있습니다.

### 6-4. PowerShell 커밋 메시지에 큰따옴표 넣지 마세요

here-string 안의 `"..."` 가 인자를 쪼개서 `error: pathspec ... did not match` 가 납니다.

### 6-5. 파일명 대소문자

Windows 로컬은 무시하지만 **GitHub 서버는 구분합니다.**

---

## 7. 사진 최적화 절차

폰 스크린샷(2340×1080)은 **좌우에 검은 레터박스**가 있습니다. 그대로 올리면 검은 띠가 보입니다.

파타야 사진 처리 실적:
- 검은 여백 제거 (좌 212px / 우 208px) → 정확히 1920×1080
- 가로 1600px 로 축소, JPEG 품질 84
- 1,010KB → 188KB / 1,869KB → 262KB

권장 기준: **가로 1600px 내외, 장당 500KB 이하.**
System.Drawing 으로 열·행별 최대 밝기를 스캔해 여백 경계를 찾는 방식이 잘 동작했습니다
(임계값 24, 4px 간격 샘플링). 사진은 `public/projects/<slug>/` 에 넣습니다.

**주의:** 대화창에 붙인 이미지는 파일로 접근할 수 없습니다. 사용자에게 파일 경로를 물어보세요.
지금까지는 `C:\Users\HP\Downloads` 에 있었습니다.

---

## 8. 남은 일

### 다음 작업 (사용자가 예고함)

- [ ] **디자인 변경.** React 전환의 원래 목적입니다. 방향은 아직 안 정해졌습니다.
      참고 사이트를 받는 것이 가장 빠릅니다.

### 우선순위 높음

- [ ] **나머지 29개 프로젝트의 사진·영상.** 지금은 파타야만 있음 (사진 2 · 영상 2)
- [ ] **전화번호 git 히스토리.** 아래 10번 항목. 사용자 결정 대기 중

### 사용자 확인 대기 (답변 못 받음)

- [ ] **집쇼코리아 재직 기간** — `2024 ~ 현재` 는 **추정값**입니다. 확인 필요
- [ ] **파타야 영상 2개의 관 이름** — 꽃 / 숲 / 파도 / 메인관 중 무엇인지 미확인.
      `youtube: [{ url, label }]` 형태로 이름을 붙일 수 있게 지원해 뒀습니다
- [ ] **기술 목록에서 Notion · Figma · Photoshop 분리 여부**
- [ ] **대표작(파타야) 목록 상단 고정 여부** — 날짜순이라 현재 11번째

---

## 9. 사용자와 일하는 방식

- **코드보다 방법을 먼저 원합니다.** 첫 요청이 "코드를 먼저 작성하지말고 방법을 알려주세요"
- **한 번에 한 단계씩.** "차근차근 갈게요"
- 문구처럼 주관적인 것은 선택지를 놓고 고르는 방식을 쓰지만, **8개를 제안했다가 전부
  반려된 적이 있습니다.** 방향이 안 잡히면 길게 늘어놓기보다 **무엇이 안 맞는지 먼저 물어보세요.**
- 기술 선택은 **장단점을 솔직하게 비교해 주면 스스로 판단합니다.**
  React 전환도 제가 "지금은 필요 없다"고 했지만 사용자가 편집 편의를 근거로 되물었고,
  그 지적이 타당해서 진행했습니다. 방어하지 말고 사실대로 답하세요.

---

## 10. ⚠️ 전화번호 — 미해결

`src/data/profile.ts` 에 전화번호를 담아 두었다가 **공개 저장소에 커밋된 상태**였습니다.
현재 파일에서는 비웠지만 **git 히스토리에는 남아 있습니다.**

- 최초 유입: 커밋 `3ac9448` (2026-08-06)
- 현재: `phone: ""` 로 제거됨
- 완전 삭제하려면 `git filter-repo` 등으로 히스토리 재작성 후 force push 필요

사용자에게 알렸고 **아직 결정되지 않았습니다.** 요청 없이 임의로 force push 하지 마세요.

앞으로도 **개인정보를 `src/data/` 에 넣지 마세요.** 이 저장소는 public 입니다.
`showPhone: false` 는 화면에 안 그린다는 뜻일 뿐, 공개되지 않는다는 뜻이 아닙니다.
