# 인수인계 — juyoung-page

다른 세션·다른 계정·다른 컴퓨터에서 이 사이트를 이어받을 때 읽는 문서입니다.

- 구조와 규칙 → [CLAUDE.md](CLAUDE.md)
- 프로젝트 추가법 → [README.md](README.md)
- 사진·영상 넣는 법 → [MEDIA.md](MEDIA.md)
- **이 문서** → 왜 그렇게 되어 있는지, 지금 상태, 남은 일, 함정

최종 갱신: 2026-08-07 — 문서 안의 파일 경로·명령·숫자·커밋을 실제 저장소와 대조해 확인했습니다.

---

## 0. 새 컴퓨터에서 시작하기

```bash
git clone https://github.com/cooldae/juyoung-page.git
cd juyoung-page
npm install
npm run dev
```

http://localhost:5220/juyoung-page/ 에서 열립니다.

**이것만으로 개발·빌드·배포가 전부 됩니다.** 저장소 밖에 있어야 하는 파일은 없습니다.
(실제로 빈 폴더에 clone 해서 `npm ci` → `npm run build` 까지 확인했고, 빌드 결과가 배포본과 동일했습니다.)

| 명령 | 내용 |
|---|---|
| `npm run dev` | 개발 서버 (5220) |
| `npm run build` | 타입 검사 + 빌드 |
| `npm run typecheck` | 타입 검사만 |
| `npm run preview` | 빌드 결과물 확인 (5221) |

### 저장소 밖에 있는 것 — 사이트 동작에는 영향 없음

`tools/` 안의 스크립트 두 개는 **이 컴퓨터의 작업 폴더**를 기본값으로 가리킵니다.

```
C:\Users\HP\Desktop\주영 작업     ← 사진·영상 원본 보관함 (저장소 아님)
```

다른 컴퓨터에서는 이 폴더가 없으므로 스크립트가 실패합니다. **정상입니다.**
필요하면 `-Root "다른경로"` 로 넘기면 되고, 사이트 자체와는 무관합니다.

---

## 1. 무엇을 만든 사이트인가

김주영(Kim Juyoung)의 **개인 포트폴리오**. 채용 담당자가 보는 것을 전제로 합니다.

| 항목 | 값 |
|---|---|
| 공개 주소 | https://cooldae.github.io/juyoung-page/ |
| 저장소 | https://github.com/cooldae/juyoung-page (public) |
| 기술 | Vite + React 19 + TypeScript + React Router 7 |
| 배포 | **GitHub Actions** (`.github/workflows/deploy.yml`) |
| Pages 설정 | Settings → Pages → Source = **GitHub Actions** (branch 방식 아님) |
| git 계정 | cooldae / yegod1101@gmail.com |

`main` 에 push 하면 자동 빌드·배포됩니다. **40초~2분.**
타입 검사나 빌드가 실패하면 배포가 멈추므로 깨진 사이트가 올라가지 않습니다.

---

## 2. 현재 상태 (2026-08-07)

| 항목 | 수 |
|---|---|
| 프로젝트 | **34** |
| 대표작 (★) | 6 |
| 사진 | **53장** / 16개 프로젝트 (프로필 사진 1장 별도) |
| 유튜브 영상 | **14개** / 9개 프로젝트 |
| 미업로드 영상 | 33개 (작업 폴더에만 있음) |
| 분류 | 게임 · 전시 인터랙션 · 드로잉 · 미디어아트 · AR · VR · IoT · 앱 · 웹 |
| 소속 구분 | 집쇼코리아(주) · 인터보이드(주) · **개인 프로젝트** |

### 대표작 6개

파타야 Pado Art Museum · 「한눈에 보는 제주도」 태블릿 앱 · 충남도청 AR 내비게이션 ·
컬러투라이프 알파벳 어드벤처 · 야외 주차 센서 프로토콜 R&D · **배고파서 낚시하는 고양이**

### 개인 프로젝트 2건

운영 중인 실제 서비스라 **채용 담당자가 클릭해서 직접 써볼 수 있는 유일한 항목**입니다.

- `hungry-cat-fishing` — https://hungrycatfishing.com (저장소 `cooldae/fishdex-web`, 비공개)
- `unbbal-lab` — https://unbbal.com (저장소 `cooldae/unbbal-web`, 비공개)

---

## 3. 이력

| 버전 | 내용 |
|---|---|
| v1 | 순수 HTML/CSS/JS, 빌드 없음 |
| v2 | React + TypeScript 전환 (디자인·콘텐츠는 그대로 두고 구조만 이관) |

**v1 로 되돌리려면**

```bash
git checkout backup/design-v1 -- .
```

태그 `v1-warm-brown`, 브랜치 `backup/design-v1`, 그리고 바탕화면의
`juyoung-page_backup_v1_2026-08-06.zip` (git 히스토리 포함) 세 곳에 남아 있습니다.

전환 이유는 디자인을 크게 바꾸고 싶어했고 코드 편집·컴포넌트 분리가 더 편해서입니다.
"React 가 디자인을 바꿔주지는 않는다"는 점은 사용자도 인지한 상태에서 결정했습니다.

---

## 4. 핵심 설계 — 왜 이렇게 만들었나

프로젝트가 34개고 계속 늘어납니다. **프로젝트마다 페이지를 만들면 안 됩니다.**

```
데이터 파일 1개  +  상세 템플릿 1개  =  프로젝트 무한개
```

- `src/data/projects.ts` 의 `PROJECTS` 배열이 **유일한 원본**
- `src/pages/ProjectDetail.tsx` 하나가 34개 상세 페이지를 전부 그림
- 필터 분류·회사 목록·상단 숫자(34/5/2022)가 전부 데이터에서 자동 계산

### 상세 페이지 블록 순서

Video → Gallery → 프로젝트 개요 → 기술 스택 → 담당 업무 및 구현 내용 →
**문제 해결 · 검증** → 상세 구현 내용 → 성과 → 비고 → 관련 링크

값이 없는 블록은 통째로 사라집니다. `research` 는 R&D 성 프로젝트용으로,
"무엇을 만들었다"(`work`) 와 "안 되던 걸 어떻게 되게 했다" 를 구분하려고 나눠 뒀습니다.
`hand-tracking-fortress` 가 그 예입니다.

**이 구조를 깨지 마세요.** 사용자의 최우선 요구가
*"제가 직접 추가할 수 있는 구조로, 업데이트가 쉽게"* 였습니다.

---

## 5. 디자인 방향 (사용자 원문)

> "따뜻한 연한 갈색 톤의 분위기에 인터렉션이 어느정도 들어간 페이지면 좋겠습니다.
> 보는 사람이 피곤하거나 불편하지 않은 느낌으로 원합니다."

- 아이보리 `#f7f2e8` / 브라운 `#3b3229` / 포인트 캐러멜 `#a97b53`
- **순백·순검정 안 씁니다.** 다크 모드도 차가운 회색이 아니라 따뜻한 다크 브라운
- **스크롤 가로채기·상시 애니메이션 금지**, `prefers-reduced-motion` 존중

### 첫 화면 인터랙션 (전부 Hero.tsx / CursorGlow.tsx)

| 요소 | 움직임 |
|---|---|
| 이름 글자 | 40ms 간격으로 하나씩 등장, 커서 105px 안쪽 글자가 코사인 감쇠로 물결 |
| 기술 태그 | 순차 등장, 커서 120px 안쪽 태그가 커서 쪽으로 최대 8px 끌려옴 (자석) |
| 배경 | 커서를 따라오는 옅은 빛 — **커서가 멈추면 같이 멈춤** |
| 숫자 | 0에서 목표값까지 카운트업 |
| 스크롤 | 첫 화면이 22% 속도로 따라오며 흐려짐, 하단 SCROLL 힌트는 40px 내리면 사라짐 |

물결과 자석이 **같은 감쇠 곡선**을 씁니다. 새 인터랙션을 넣을 때도 맞추면 페이지가 한 언어로 읽힙니다.

---

## 6. 진행 중 내려진 결정과 이유

| 결정 | 이유 |
|---|---|
| 큰 글자 = `KIM JUYOUNG 김주영` | 추상적 문구를 8개 제안했으나 전부 반려. 사용자가 직접 정함 |
| eyebrow = 무슨 일을 하는지 | 큰 글자가 이름이라 직무 설명이 필요해짐 |
| hero 를 74svh 로 제한 | 화면을 꽉 채우면 아래에 뭐가 있는지 안 보임. 지금은 Projects 가 177px 비침 |
| Projects 를 About 보다 위로 | 이 사이트의 본론이 프로젝트라서 |
| Skills 를 hero 에만 | About 에 또 넣으면 같은 태그 20개가 두 번 나옴. **다시 만들지 마세요** |
| 필터를 회사 → 분류로 | 일하는 방식이 실제로 갈리는 지점으로 묶음 |
| 분류를 여러 개 붙임 | 컬러투라이프는 게임이자 드로잉, DMZ 포토존은 전시이자 AR |
| 개인 프로젝트를 `company` 로 구분 | 회사 프로젝트는 "~ 참여" 가 많음. 섞으면 단독 작업인지 흐려짐 |
| 새로고침하면 첫 화면으로 | 사용자 요청. 공유 링크는 살아 있도록 세션 표시로 구분 |
| `youtube-nocookie.com` | 재생 전까지 유튜브 추적 쿠키가 안 심김 |
| 헤더 메뉴를 onClick 으로 처리 | 해시가 안 바뀌면 같은 메뉴 재클릭이 무시됨 + 고정 헤더에 제목이 가림 |

---

## 7. 함정 — 반드시 알아야 할 것

### 7-1. `vite.config.ts` 의 `base` 를 건드리지 마세요

`/juyoung-page/` 하위 경로 배포입니다. 틀리면 CSS·JS·이미지가 전부 404 납니다.
이미지 경로는 `src/lib/media.ts` 의 `imageSrc()` 로만 만드세요.

### 7-2. `public/404.html` 을 지우지 마세요

GitHub Pages 는 `/project/xxx` 를 모릅니다.

```
/juyoung-page/project/pado-art-museum
  → Pages 가 404 + 404.html 반환
  → 404.html 이 /juyoung-page/?redirect=project%2Fpado-art-museum 로 이동
  → src/main.tsx 가 진짜 주소로 복원
```

**콘솔에 404 가 한 번 찍히는 것은 정상입니다.**

⚠️ 여기서 버그가 났었습니다. 404.html 은 쿼리의 `?` 를 `&` 로 바꿔 넘깁니다
(`project.html&id=xxx`). 복원할 때 첫 `&` 를 `?` 로 되돌리지 않으면 경로에 `&` 가
그대로 남아 아무 프로젝트도 안 잡힙니다. `src/main.tsx` 의 `normalizeUrl()` 참고.

### 7-3. 브라우저·Pages 캐시 때문에 자주 헷갈립니다

배포는 됐는데 화면이 안 바뀌는 일이 **매우 자주** 있습니다.
**서버가 주는 파일을 먼저 확인하세요.** 화면만 보고 "배포 실패" 라고 결론 내리면 틀립니다.

```powershell
(Invoke-WebRequest "https://cooldae.github.io/juyoung-page/" -UseBasicParsing).Content -match 'index-\w+\.js'
```

브라우저에서는 주소에 `?cb=아무숫자` 를 붙이면 캐시를 우회합니다.

### 7-4. PowerShell 에서 한글 스크립트는 BOM 이 필요합니다

Windows PowerShell 5.1 은 BOM 없는 UTF-8 을 ANSI 로 읽습니다. 한글이 깨지고
`${변수}` 치환까지 실패합니다. `tools/*.ps1` 은 BOM 을 붙여 저장돼 있으니
편집기에서 저장할 때 인코딩을 바꾸지 마세요.

### 7-5. PowerShell 은 `[ ]` 를 와일드카드로 봅니다

`영상 [2]` 같은 폴더나 `04-02_[16 33 18]c.mp4` 같은 파일을 다룰 때
`-LiteralPath` 를 안 쓰면 **조용히 실패합니다.** 실제로 파일 하나가 옮겨지지 않았는데
성공한 것처럼 출력된 적이 있습니다.

### 7-6. 커밋 메시지에 큰따옴표를 넣지 마세요

here-string 안의 `"..."` 가 인자를 쪼개서 `error: pathspec ... did not match` 가 납니다.

### 7-7. 파일명 대소문자

Windows 로컬은 무시하지만 **GitHub 서버는 구분합니다.**

---

## 8. 사진·영상 작업 흐름

자세한 절차는 [MEDIA.md](MEDIA.md) 에 있습니다. 요점만:

```
바탕화면\주영 작업\<프로젝트> [이미지, 페이지완료, 유튜브만, 미업로드]
├─ 사진 업로드      사이트에 올라간 사진 (1600px 이하, 500KB 이하)
├─ 사진 미업로드    아직 안 올린 사진
├─ 사진 원본        원본 고화질
├─ 영상 미업로드    아직 유튜브에 안 올린 영상
├─ 유튜브 업로드 완료
└─ 페이지 업로드 완료
```

- `tools/sync-folder-names.ps1` — 폴더 정리 + 이름 부여 + 사이트 반영 + 폴더명 갱신
- `tools/optimize-images.ps1` — 사진 하나짜리 폴더를 처리할 때

**새 프로젝트 폴더를 만들면 `sync-folder-names.ps1` 위쪽 `$MAP` 에 추가해야 합니다.**
매핑이 없으면 "매핑 없는 폴더" 로 보고만 하고 건너뜁니다.

사진은 기본적으로 1600px JPEG 로 줄이지만, **흐름도·도표처럼 글자와 얇은 선이 많은 PNG 는
그대로 둡니다** (500KB 이하 · 가로 1600px 이하면 원본 복사). JPEG 로 바꾸면 글자가 뭉개집니다.
`socket-zombie-shooting` 의 두 장이 그 경우입니다.

파일은 폴더 최상위에 던져도 되고 `사진 원본` 에 넣어도 됩니다. 둘 다 집어갑니다.

**핵심 규칙: 이미 `project_<slug>_<번호>` 가 붙은 파일 이름은 절대 다시 매기지 않습니다.**
유튜브 링크가 어긋나기 때문입니다. 데이터 파일의 `// project_xxx_N.mp4` 주석이
"이 영상은 페이지에 올라갔다" 는 표시이고, 스크립트가 그걸 읽어 파일을 옮깁니다.

**유튜브에 올린 뒤에는 주소만 주면 됩니다.** 유튜브 제목으로 어느 로컬 파일인지
역추적할 수 있습니다 (제목이 파일명과 같게 올라가 있음).

---

## 9. 남은 일

### 우선순위 높음

- [ ] **영상 33개 미업로드.** 파타야 11 · 알파벳어드벤쳐 8 · 컬러투라이프-CES 6 ·
      터치디자이너 3 · 소래역사관 2 · 충남도청 1 · 소켓 다트 1 · 컬러투라이프 함안 1
      (유튜브 일일 업로드 제한 때문에 나눠서 올리는 중)
- [ ] **사진 없는 프로젝트 16개.** 특히 대표작인 `color2life-alphabet` 이 사진 0장이라
      목록 카드가 글자만으로 보입니다
- [ ] **레이싱·다트 게임 흐름도.** 좀비 슈팅과 구조가 같아서 비슷한 도표가 있을 수 있습니다
- [ ] **전화번호 git 히스토리.** 아래 11번 항목. 사용자 결정 대기 중

### 확인 대기

- [ ] **파타야 유튜브 2개의 로컬 파일 미확인.** 이름 규칙을 정하기 전에 올린 영상이라
      `project_pado-art-museum_1~11.mp4` 중 어느 것인지 모릅니다. 그래서 파타야 폴더는
      `[6, 0, 0, 11]` 로 표시되고 11개가 전부 미업로드로 잡혀 있습니다 (실제로는 2개가 이미 올라감)
- [ ] **집쇼코리아 재직 기간** — `2024 ~ 현재` 는 **추정값**
- [ ] **개인 프로젝트 2건의 정확한 기간·스택** — `2026` 으로만 적혀 있고
      운빨실험실은 프레임워크가 확인 안 돼 `JavaScript` 로만 표기
- [ ] **기술 목록에서 Notion · Figma · Photoshop 분리 여부**

### 선택

- [ ] hero 소개 문단 (`profile.ts` 의 `intro`) — 지금은 비어 있음
- [ ] 사진 캡션 기능 — 현재 미지원. `images: [{ file, caption }]` 로 확장 가능
- [ ] 개인 프로젝트 저장소 공개 시 코드 링크 추가

---

## 10. 사용자와 일하는 방식

- **코드보다 방법을 먼저 원합니다.** 첫 요청이 "코드를 먼저 작성하지말고 방법을 알려주세요"
- **한 번에 한 단계씩.** "차근차근 갈게요"
- 문구처럼 주관적인 것은 선택지를 놓고 고르지만, **8개를 제안했다가 전부 반려된 적이 있습니다.**
  방향이 안 잡히면 길게 늘어놓기보다 **무엇이 안 맞는지 먼저 물어보세요.**
- 기술 선택은 **장단점을 솔직하게 비교해 주면 스스로 판단합니다.** React 전환도
  제가 "지금은 필요 없다" 고 했지만 사용자가 편집 편의를 근거로 되물었고, 그 지적이
  타당해서 진행했습니다. 방어하지 말고 사실대로 답하세요.
- **작업 폴더의 파일을 직접 정리하기도 합니다.** 파일이 사라지거나 번호가 바뀌어 있으면
  버그를 의심하기 전에 먼저 물어보세요. 실제로 그런 적이 있습니다.

---

## 11. ⚠️ 전화번호 — 미해결

`src/data/profile.ts` 에 전화번호를 담아 두었다가 **공개 저장소에 커밋된 상태**였습니다.
현재 파일에서는 비웠지만 **git 히스토리에는 남아 있습니다.**

- 들어간 커밋: `3ac9448` (유입) · `887f91a` (제거) — 이 둘 사이 구간에 남아 있습니다
- 현재 버전: `phone: ""` — `git grep "8222-8762" HEAD` 로 확인했고 나오지 않습니다
- 완전 삭제하려면 `git filter-repo` 등으로 히스토리 재작성 후 force push 필요

> 참고로 `git log -S "8222"` 는 PNG 파일의 우연한 바이트열까지 잡습니다.
> 실제로 찾을 때는 `-S "8222-8762"` 처럼 구분자까지 넣으세요.

사용자에게 알렸고 **아직 결정되지 않았습니다.** 요청 없이 임의로 force push 하지 마세요.

**개인정보를 `src/data/` 에 넣지 마세요.** 이 저장소는 public 입니다.
`showPhone: false` 는 화면에 안 그린다는 뜻일 뿐, 공개되지 않는다는 뜻이 아닙니다.
