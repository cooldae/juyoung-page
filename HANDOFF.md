# 인수인계 — juyoung-page

다른 세션·다른 계정에서 이 사이트를 이어받을 때 읽는 문서입니다.
구조와 규칙은 [CLAUDE.md](CLAUDE.md) 에 있고, 이 문서는 **왜 그렇게 되어 있는지와 남은 일**을 다룹니다.

작성 시점: 2026-08-06

---

## 0. 세션 시작할 때

**작업 폴더를 `juyoung-web` 이 아니라 `juyoung-web/juyoung-page` 로 열어야 합니다.**
상위 폴더는 여러 사이트를 담는 컨테이너이고, 다른 사이트는 다른 세션에서 작업 중일 수 있습니다.
상위 폴더를 기준으로 열면 `preview_start` 가 `.claude/launch.json` 을 못 찾습니다.

```
C:\Users\HP\Desktop\juyoung-web\juyoung-page      ← 여기
```

---

## 1. 무엇을 만든 사이트인가

김주영(Kim Juyoung)의 **개인 포트폴리오**. 채용 담당자가 보는 것을 전제로 합니다.
Unity 기반 전시 인터랙션 / AR·VR / IoT 프로젝트 **30건**의 기록이 핵심입니다.

| 항목 | 값 |
|---|---|
| 공개 주소 | https://cooldae.github.io/juyoung-page/ |
| 저장소 | https://github.com/cooldae/juyoung-page (public) |
| 배포 | GitHub Pages, `main` 브랜치 `/ (root)` 직접 서빙 |
| 빌드 | **없음.** 순수 HTML / CSS / JS |
| 로컬 서버 | 포트 5220 (`npx serve . -l 5220`) |
| git 계정 | cooldae / yegod1101@gmail.com (전역 설정됨) |

`main` 에 push 하면 자동 반영됩니다. **다만 실제로 반영되기까지 3~9분 걸립니다.**
바로 확인하려 하지 말고 아래 방법으로 기다리세요.

```powershell
$u="https://cooldae.github.io/juyoung-page/data/projects.js"
for ($i=1; $i -le 45; $i++) { try { $r=Invoke-WebRequest -Uri $u -UseBasicParsing -TimeoutSec 15; if ($r.Content -match '찾을문자열') { "DEPLOYED"; break } } catch {}; Start-Sleep -Seconds 12 }
```

---

## 2. 핵심 설계 — 왜 이렇게 만들었나

프로젝트가 30개입니다. **프로젝트마다 HTML 파일을 만들면 30개 파일이 되고, 디자인을 한 번 고치려면 30군데를 손봐야 합니다.**

그래서 이렇게 갈랐습니다.

```
데이터 파일 1개  +  상세 템플릿 1개  =  프로젝트 무한개
```

- `data/projects.js` 의 `window.PROJECTS` 배열이 **유일한 원본**
- `index.html` 이 목록을, `project.html?id=<slug>` 이 상세를 렌더링
- 데이터는 `fetch` 가 아니라 `<script src>` 로 읽습니다 → **`file://` 로 열어도 동작**하고 CORS 문제가 없습니다

**이 구조를 깨지 마세요.** 사용자의 최우선 요구가 "제가 직접 추가할 수 있는 구조, 업데이트가 쉽게" 였습니다.
프로젝트 추가는 `data/projects.js` 에 블록 하나 붙여넣는 것으로 끝나야 합니다.

### 파일 구성

```
juyoung-page/
├─ index.html              메인 (hero + About + 프로젝트 목록)
├─ project.html            상세 템플릿 (?id=<slug>)
├─ data/
│   ├─ profile.js          이름·연락처·경력·강점·스킬·학력
│   └─ projects.js         ★ 프로젝트 30건. 보통 여기만 고침
├─ assets/
│   ├─ css/style.css       색은 :root 변수만 고치면 전체 반영
│   ├─ js/common.js        이스케이프 · 유튜브 파싱 · 정렬 · reveal · 푸터
│   ├─ js/index.js         목록 렌더링 + 필터 + 검색
│   ├─ js/project.js       상세 렌더링 + 영상 넘기기 + 라이트박스
│   └─ projects/<slug>/    프로젝트별 사진
├─ .claude/launch.json     dev 서버 (5220)
├─ serve.json              로컬 서버 설정 — 지우지 말 것 (5번 항목)
├─ .nojekyll               지우지 말 것
├─ CLAUDE.md               구조 / 규칙
└─ HANDOFF.md              이 문서
```

---

## 3. 디자인 방향 (사용자가 직접 요청한 것)

> "따뜻한 연한 갈색 톤의 분위기에 인터랙션이 어느정도 들어간 페이지면 좋겠습니다.
> 보는 사람이 피곤하거나 불편하지 않은 느낌으로 원합니다."

- 아이보리 `#f7f2e8` / 브라운 `#3b3229` / 포인트 캐러멜 `#a97b53`
- **순백·순검정 안 씁니다.** 눈 피로 때문입니다
- 다크 모드도 차가운 회색이 아니라 **따뜻한 다크 브라운**
- 인터랙션은 절제 — 스크롤 페이드업, 카드 호버, 이미지 라이트박스, 영상 넘기기 정도
- **스크롤 가로채기 · 상시 애니메이션 금지.** "피곤하지 않게" 요청에 정면으로 어긋납니다
- `prefers-reduced-motion` 존중 (CSS 하단에 처리되어 있음)

---

## 4. 진행 중 내려진 결정과 이유

| 결정 | 이유 |
|---|---|
| hero 큰 문장 = `김주영의 포트폴리오` | 추상적인 문구를 8개 제안했으나 전부 반려. 사용자가 직접 정한 방향 |
| hero 소개 문단 없음 (`intro: ""`) | 위 결정에 따라 비움. 값을 넣으면 다시 나타남 |
| Skills 목록을 hero 에 표시 | 사용자 요청 ("서브는 사용했던 기술을 목록으로") |
| About 에서 Skills 카드 제거 | 같은 20개 태그가 한 페이지에 두 번 나오게 되어서. **다시 만들지 마세요** |
| 언어(한국어·영어·일본어) 제거 | 사용자 요청 |
| 학력을 About 카드로, 학위를 위에 | 채용 기본 확인 항목이지만 주인공은 프로젝트. 순서가 곧 무게 |
| 영상 화살표를 플레이어 아래 배치 | 위에 겹치면 유튜브 자체 컨트롤을 가림 |
| `youtube-nocookie.com` 사용 | 재생 전까지 유튜브 추적 쿠키가 안 심김 |
| 정렬은 `start` 내림차순 + 배열 순서 | 같은 `start` 값이면 파일에 적힌 순서를 따름 (JS sort 안정성) |

---

## 5. 함정 — 반드시 알아야 할 것

### 5-1. 하위 경로 배포 → 절대 경로 금지

루트가 아니라 `/juyoung-page/` 아래에서 열립니다. `/` 로 시작하는 경로는 배포본에서 전부 깨집니다.

```html
<img src="/assets/x.jpg">     <!-- ❌ 404 -->
<img src="assets/x.jpg">      <!-- ✅ -->
```

### 5-2. `serve.json` 을 지우지 마세요

로컬 `npx serve` 는 기본값으로 `project.html` → `project` 리다이렉트하면서
**쿼리스트링(`?id=...`)을 날려버립니다.** 상세 페이지가 "찾을 수 없음"으로 뜹니다.
GitHub Pages 는 이런 변환을 하지 않으므로 `cleanUrls: false` 로 꺼두었습니다.

### 5-3. 브라우저가 301 을 캐시합니다

위 문제를 겪은 뒤에는 브라우저가 **301 영구 리다이렉트를 캐시**해서, 서버를 고쳐도 계속 실패합니다.
증상: 주소가 `/project` 로 바뀌며 "찾을 수 없음". 해결: `Ctrl+Shift+R` 또는 쿼리 추가(`&cb=1`).

### 5-4. GitHub Pages 가 JS·CSS 를 캐시합니다

배포는 됐는데 화면이 안 바뀌는 일이 자주 있습니다. 약 10분입니다.
**서버 내용을 먼저 확인하고 판단하세요.** 브라우저 화면만 보고 "배포 실패"라고 결론 내리면 틀립니다.

```js
fetch('data/profile.js', {cache:'reload'}).then(r=>r.text()).then(t=>t.includes('찾을문자열'))
```

### 5-5. PowerShell 커밋 메시지에 큰따옴표 넣지 마세요

here-string 안의 `"..."` 가 인자를 쪼개서 `error: pathspec ... did not match` 가 납니다.
커밋 메시지는 따옴표 없이 쓰세요.

### 5-6. 파일명 대소문자

Windows 로컬은 무시하지만 **GitHub 서버는 구분합니다.** `01.JPG` 와 `01.jpg` 는 다른 파일입니다.

### 5-7. 텍스트는 반드시 `App.esc()` 통과

데이터에 `R&D`, `HTML & CSS & JS` 같은 문자열이 있습니다. 이스케이프 안 하면 깨집니다.
새 렌더링 코드를 추가할 때도 반드시 통과시키세요.

---

## 6. 사진 최적화 절차

폰 스크린샷(2340×1080)은 **좌우에 검은 레터박스**가 있습니다. 그대로 올리면 페이지에 검은 띠가 보입니다.

파타야 사진 처리 실적:
- 검은 여백 제거 (좌 212px / 우 208px) → 정확히 1920×1080
- 가로 1600px 로 축소, JPEG 품질 84
- 1,010KB → 188KB / 1,869KB → 262KB

권장 기준: **가로 1600px 내외, 장당 500KB 이하.**
System.Drawing 으로 열별 최대 밝기를 스캔해 여백 경계를 찾는 방식이 잘 동작했습니다.
(임계값 24, 열/행 4px 간격 샘플링)

**주의:** 대화창에 붙인 이미지는 파일로 접근할 수 없습니다. 사용자에게 파일 경로를 물어보세요.
지금까지는 `C:\Users\HP\Downloads` 에 있었습니다.

---

## 7. 남은 일

### 우선순위 높음

- [ ] **나머지 29개 프로젝트의 사진·영상.** 지금은 파타야만 있음 (사진 2 · 영상 2).
      전부 채울 필요 없고 대표작 서너 개만 있어도 인상이 크게 달라집니다.
- [ ] **전화번호 git 히스토리.** 아래 9번 항목 참고. 사용자 결정 대기 중.

### 사용자 확인 대기

- [ ] **집쇼코리아 재직 기간** — `2024 ~ 현재` 로 적었으나 프로젝트 날짜와 이메일 도메인으로 **추정한 값**입니다. 확인 필요.
- [ ] **파타야 영상 2개의 관 이름** — 꽃 관 / 숲 관 / 파도 관 / 메인관 중 무엇인지 미확인.
      `youtube: [{ url, label }]` 형태로 이름을 붙일 수 있게 이미 지원해 뒀습니다.
- [ ] **기술 목록에서 Notion · Figma · Photoshop 분리 여부** — 개발 기술과 협업 도구가 섞여 보인다고 지적했고, `개발` / `협업 도구` 로 나누는 안을 제시했으나 답변 없음.
- [ ] **대표작(파타야) 목록 상단 고정 여부** — 날짜순이라 현재 11번째. 제안했으나 답변 없음.

### 선택

- [ ] hero 소개 문단 (`profile.js` 의 `intro`) — 지금은 비어 있음. 문구가 생기면 넣으면 됨
- [ ] 프로젝트 사진에 캡션 기능 — 현재 미지원. 필요하면 `images: [{ file, caption }]` 형태로 확장 가능

---

## 8. 사용자와 일하는 방식

지금까지 관찰된 것입니다. 참고만 하세요.

- **코드보다 방법을 먼저 원합니다.** 첫 요청이 "코드를 먼저 작성하지말고 방법을 알려주세요" 였습니다.
- **한 번에 한 단계씩.** "차근차근 갈게요."
- 문구 같은 주관적인 것은 **선택지를 여러 개 놓고 고르는 방식**을 씁니다. 다만 8개를 제안했다가 전부 반려된 적이 있으니, 방향이 안 잡히면 **길게 늘어놓기보다 무엇이 안 맞는지 먼저 물어보세요.**
- 최종적으로 사용자가 직접 방향을 정하는 경우가 많습니다. 제안은 하되 붙들지 마세요.

---

## 9. ⚠️ 전화번호 — 미해결

`data/profile.js` 에 전화번호를 담아 두었다가 **공개 저장소에 커밋된 상태**였습니다.
현재 파일에서는 값을 비웠지만, **git 히스토리에는 남아 있습니다.**

- 최초 유입: 커밋 `3ac9448` (2026-08-06)
- 현재: `data/profile.js` 의 `phone: ""` 로 제거됨
- **히스토리에서 완전히 지우려면 `git filter-repo` 등으로 재작성 후 force push 필요**

사용자에게 알렸고, 히스토리 재작성 여부는 **아직 결정되지 않았습니다.**
이어받는 세션에서 사용자가 요청하면 진행하세요. 요청 없이 임의로 force push 하지 마세요.

앞으로도 **개인정보를 `data/` 파일에 넣지 마세요.** 이 저장소는 public 입니다.
`showPhone: false` 는 화면에 안 그린다는 뜻일 뿐, 공개되지 않는다는 뜻이 아닙니다.
